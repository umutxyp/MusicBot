"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.applyToEmitter = void 0;
const Decorators_1 = require("./Decorators");
const applyToEmitter = (emitter, opts) => {
    if (!Array.isArray(opts))
        opts = opts[Decorators_1.eventMapper];
    opts.forEach(opt => {
        emitter[opt.type](opt.event, typeof opt.method === 'string'
            ? emitter[opt.method].bind(emitter)
            : opt.method);
    });
};
exports.applyToEmitter = applyToEmitter;
